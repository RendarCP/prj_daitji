# Backend Convention

- Follow `app/AGENTS.md`, `lib/AGENTS.md`, and `supabase/AGENTS.md` for scoped rules.
- Route handlers use `route.ts` with explicit HTTP method exports.
- Validate server-side inputs.
- Enforce auth, authorization, and RLS assumptions.
- Document API, DB, and error response changes for Frontend Agent and Test Agent.
